#include <stdio.h>
int main()
    { int N,pro=1,i=1;
      scanf("%d",&N);
      while(i<=N)
        {   
            pro=pro*i;
            i++;
        }
       printf("%d",pro);
       return 0;
     }  
